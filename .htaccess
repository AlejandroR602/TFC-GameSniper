RewriteEngine On
RewriteBase /GameSniper/

# Protect sensitive directories
RewriteRule ^(app|config|core|database)/ - [F,L]

# Redirect root requests to public/
RewriteCond %{REQUEST_URI} !^/GameSniper/public/
RewriteRule ^(.*)$ public/$1 [L,QSA]
